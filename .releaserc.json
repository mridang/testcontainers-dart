{
  "branches": [
    "master"
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "perl -pi -e 's/^version: .*/version: ${nextRelease.version}/' packages/testcontainers_core/pubspec.yaml packages/testcontainers_compose/pubspec.yaml && perl -pi -e \"s/const String tcVersion = '[^']*'/const String tcVersion = '${nextRelease.version}'/\" packages/testcontainers_core/lib/src/labels.dart"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": []
      }
    ],
    [
      "@semantic-release/git",
      {
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}",
        "assets": [
          "packages/testcontainers_core/pubspec.yaml",
          "packages/testcontainers_compose/pubspec.yaml",
          "packages/testcontainers_core/lib/src/labels.dart"
        ]
      }
    ]
  ]
}
